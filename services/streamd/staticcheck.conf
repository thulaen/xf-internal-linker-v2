# Slice 1.5 — staticcheck overrides for streamd.
#
# SA1019 (deprecated symbol) is silenced because grpc-go v1.66 deprecated
# DialContext + WithBlock in favour of NewClient, but the migration path
# requires re-engineering the Unix-socket custom dialer for both server
# and healthcheck binaries. Tracked for a follow-up slice.
#
# Documentation: https://staticcheck.dev/docs/configuration/options/

# SA1019: deprecated grpc.DialContext + WithBlock — see header comment.
# ST1000: package comments missing on pre-existing internal/state and
# internal/timeflow; slice 1.5 does not modify those files.
checks = ["all", "-SA1019", "-ST1000"]
