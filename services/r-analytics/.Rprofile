# Rprofile for xf-internal-linker-v2 R analytics service
# Initializes renv if present

if (file.exists("renv/activate.R")) source("renv/activate.R")

message("XF Internal Linker V2 - R Analytics Service loaded.")
