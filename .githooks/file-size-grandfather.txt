# Grandfather list — files that exceed the 1,500-line cap and are
# being actively decomposed. Format: path:current_line_count
#
# Rules:
#   1. A file in this list may only SHRINK below its recorded baseline.
#      The hook (check-file-size.py) blocks commits that grow a
#      grandfathered file past its baseline.
#   2. Once a file is split below the global 1,500-line cap, REMOVE
#      its entry. The hook will then enforce the cap on it.
#   3. Do NOT add new entries. The cap exists to prevent god classes;
#      a fresh file should never need this list. New files always start
#      under the cap.
#
# Current entries (2026-05-10, recorded for the prevention-cleanup batch):
# views.py removed 2026-05-10 (slice 5): shared helpers extracted into
# services/settings_helpers.py; views.py is now 381 lines (well below cap).
# settings_helpers.py removed 2026-05-10 (slice 6): split into 4 layers;
# facade is now 120 lines (well below cap).
backend/apps/core/views_capacity.py:2035
backend/apps/core/views_settings.py:1437
backend/apps/pipeline/tasks.py:3594
# settings.component.ts dropped to 3380 lines (was 3889) after the
# Ranking Weights tab moved to <app-ranking-weights-tab> and 23 dead
# per-card save handlers + 5 dead helper methods were removed.
frontend/src/app/settings/settings.component.ts:3380
# settings.component.html removed 2026-05-10: dropped to 191 lines
# after the Ranking Weights tab moved to <app-ranking-weights-tab>;
# every tab now references an <app-*-tab> child, so the parent
# template is far below the 1500-line cap and the hook now enforces
# it.
