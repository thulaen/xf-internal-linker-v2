[tool.mutmut]
paths_to_mutate = [
  "apps",
]
also_copy = ["apps", "config"]
do_not_mutate = [
  "*/migrations/*",
  "*/test_*.py",
  "*/tests.py",
  "*/tests/*",
]
pytest_add_cli_args = ["-o", "addopts="]
pytest_add_cli_args_test_selection = [
  "apps",
]
