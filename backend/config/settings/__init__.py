# Settings package — choose via DJANGO_SETTINGS_MODULE environment variable.
# development.py  → local Docker dev (DEBUG=True, relaxed security)
# production.py   → production deployment (DEBUG=False, strict security)
