# Banned tokens — any text that must NOT reappear anywhere in the repo
# except in paths listed as exclusions in check_phantom_references.py.
#
# Format: one token per line. Blank lines and lines starting with # are ignored.
# Tokens are matched as word-boundary regex (\btoken\b) so short acronyms
# like `pmi` do not false-positive on `api_pmi_util` or similar.
#
# When a slice deletes a feature, append every surviving identifier that
# should never be re-introduced. A future AI session scanning the repo will
# get a loud CI failure if it tries to resurrect a deleted prefix.

# ── PR-A slice 1 — FR-225 Meta Tournament ──────────────────────────────
MetaTournamentResult
HoldoutQuery
MetaTournamentView
MetaTournamentRunView
MetaTournamentPinView
MetaTournamentComponent
meta_rotation_scheduler
meta_slot_registry
meta_rotation_tournament
meta-rotation-tournament
meta-tournament
META_SLOT_REGISTRY
MetaSlotConfig
run_meta_tournament
_evaluate_meta_on_holdout
_should_promote
FR-225
fr225-meta-rotation-scheduler

# ── PR-A slice 2 — 3 unwired C++ kernels + 5 stale specs ───────────────
strpool
inv_index
pulse_metrics
strpool_core
inv_index_core
pulse_metrics_core
fr091-cpp-extension-retrofit
opt-73-abseil-hashmap-inv-index
opt-75-abseil-hashmap-strpool
opt-78-abseil-mutex
opt-89-pipeline-accel
FR-091
OPT-73
OPT-75
OPT-78
OPT-89

# ── PR-A slice 4 — 126 pending ranking signals (Block A-O, FR-099..FR-224) ─
# 4 phase-2 signal weight files deleted + 126 matching spec files.
# All were forward-declared, never wired, and fell into one of four
# conflict/overlap/niche tiers per the plan. See docs/DELETED-FEATURES.md.

# Block A (FR-099..107) — classical IR scoring alternatives to BM25 and QL
bm25_plus
bm25l
dfr_pl2
dfr_inl2
dfr_dph
axiomatic_f2exp
two_stage_lm
plm
rm3

# Block B (FR-108..115) — proximity & dependence models overlapping phrase_relevance
sdm
wsdm
fdm
bm25tp
minspan
osp
boolprox
mrf_per_field

# Block C (FR-116..125) — HITS/TrustRank and graph-authority alternatives
# NOTE: `trustrank` un-banned 2026-04-22 — PR-M ships
# apps.pipeline.services.trustrank as the legitimate Gyöngyi 2004 helper
# (plan pick #30). The banned pending signal of the same name was never
# implemented; this identifier now means the Gyöngyi VLDB algorithm.
hits_authority
hits_hub
anti_trustrank
salsa
simrank
katz
k_shell
harmonic_centrality
leaderrank

# Block D (FR-126..133) — diversification alternatives fighting MMR.
# NOTE: `portfolio` is NOT banned — it's a common English word that
# legitimately appears in anchor-diversity discussions (FR-045 etc.).
ia_select
xquad
pm2
dpp
submodular_coverage
ldm
quota_diversity

# Block E (FR-134..143) — time-series / change detection (no consumer)
kleinberg_burst
pelt_changepoint
cusum_anomaly
stl_decomposition
mann_kendall
theil_sen
fourier_periodicity
acf_lag_k
pacf_lag_k
ewma_smoothed

# Block F (FR-144..151) — streaming sketches (duplicates or overlaps).
# NOTE: `hyperloglog` is NOT banned — it's the name of the Source-layer
# implementation we actually shipped (apps.sources.hyperloglog, PR-C).
# Only the FR-144/145 pending-backlog variants were deleted. `hyperloglog_pp`
# (HLL++) remains banned because we did not ship the extended-precision
# variant.
hyperloglog_pp
countmin_anchor
count_sketch
space_saving
t_digest
lossy_counting
b_bit_minhash

# Block G (FR-152..161) — linguistic/style signals overlapping readability/spaCy.
# NOTE: `cohesion`, `hedging`, `nominalization` are NOT banned — they're
# common linguistic/English words that appear in research citations
# (e.g. LexRank paper's "salience and cohesion") and would false-positive.
passive_voice
discourse_conn
pos_diversity
sent_variance
yule_k
mtld
punct_entropy

# Block H (FR-162..169) — click-model alternatives fighting Cascade Click
ccm
dbn
ubm
pbm
dcm
ccm_bayes
click_walk
reg_em

# Block I (FR-170..177) — query-difficulty predictors overlapping Uncertainty Sampling
query_clarity
wig
nqc
scs
query_scope
query_feedback
avgictf
scq

# Block J (FR-178..185) — info-theoretic divergences overlapping PMI/cosine
# NOTE: `pmi` un-banned 2026-04-22 — PR-E ships apps.sources.collocations.pmi
# (Church & Hanks 1990) as a legitimate helper. The original pending signal
# that lived under "pmi" was never implemented; only the Church-Hanks
# collocation scorer uses this identifier now.
# NOTE: `npmi` un-banned 2026-04-22 — PR-E also ships
# apps.sources.collocations.normalised_pmi (Bouma 2009). The identifier
# appears in spec pick-24-pmi-collocations.md citing the Bouma NPMI paper URL.
# Previously-pending signal with the same name was never implemented.
llr
kl_div
js_div
renyi_div
hellinger
wmd

# Block K (FR-186..194) — host-level authority variants overlapping PageRank/spam_guard
site_pagerank
host_trustrank
spamrank
badrank
host_age
subdomain_diversity
doorway_detector
block_pagerank
host_cohesion

# Block L (FR-195..203) — anti-spam detectors overlapping META-25 spam_guard.
# NOTE: `link_farm` and `keyword_stuffing` are NOT banned — those names belong
# to the live runtime anti-spam signals (see recommended_weights.py). Only the
# phase-2 duplicates of those names were deleted.
link_naturalness
cloaking
content_spin
sybil
astroturf
clickbait
content_farm

# Block M (FR-204..212) — author reputation overlapping PageRank/post_quality/LDA
author_h_index
co_authorship_pr
account_age_gravity
edit_history_density
mod_endorsement
rqr
topic_consistency
trust_propagation
eigentrust

# Block N (FR-213..220) — SEO / accessibility metrics (no consumer)
heading_hierarchy
alt_text_coverage
schema_completeness
open_graph_completeness
mobile_friendly
cwv_lcp
cwv_cls
cwv_inp

# Block O (FR-221..224) — passage segmentation alternatives fighting Callan 1994
texttiling
c99_segmentation
dotplot_segmentation
bayesseg

# ── PR-A slice 5 — 238 pending meta-algo specs + 5 phase-2 weight files ────
# All META-40..META-249 spec files removed except the 8 that map to the
# 52-pick roster (META-43, 55, 77, 87, 91, 96, 102, 103). The 5 phase-2
# meta weight files (_p1_p6, _p7_p12, _q1_q8, _q9_q16, _q17_q24) deleted.
# Future PR-B..P will re-seed roster picks with fresh specs and weight
# entries directly in recommended_weights.py as each is implemented.

# Phase-2 meta weight file stems (removed)
recommended_weights_phase2_metas_p1_p6
recommended_weights_phase2_metas_p7_p12
recommended_weights_phase2_metas_q1_q8
recommended_weights_phase2_metas_q9_q16
recommended_weights_phase2_metas_q17_q24
FORWARD_DECLARED_WEIGHTS_PHASE2_METAS_P1_P6
FORWARD_DECLARED_WEIGHTS_PHASE2_METAS_P7_P12
FORWARD_DECLARED_WEIGHTS_PHASE2_METAS_Q1_Q8
FORWARD_DECLARED_WEIGHTS_PHASE2_METAS_Q9_Q16
FORWARD_DECLARED_WEIGHTS_PHASE2_METAS_Q17_Q24

# ── PR-A slice 7 — dev frontend compose setup retired (2026-04-22) ────
# Only the prod Angular bundle (xf-linker-frontend-prod) behind nginx is
# supported now. No ng-serve-based dev frontend, no override/prod compose
# split. See docs/DELETED-FEATURES.md.
#
# Identifiers below include the auto-generated docker-compose image name
# (`xf-internal-linker-v2-frontend`) and the dev container name
# (`xf_linker_frontend`) — re-adding a `frontend:` service to docker-compose
# would regenerate those, and we want the gate to catch that regression
# even before anyone notices the image on disk.
frontend-dev
xf_linker_frontend_dev
xf_linker_frontend
xf-internal-linker-v2-frontend
docker-compose.override.yml
docker-compose.prod.yml
