# ruff: noqa
"""Add the STATUS_STICKY value to PaperTrailEntry.status choices.

Sticky entries are out-of-band standing-orders documents that ship with
an abstract up to 10,000 words (compared with the 1,200-word cap on every
other status). Sticky #1 of the Paper Trail is the Spec-Driven Gradual
Rewrite Policy. See docs/specs/fr-sticky-1-read-rule.md for the rule
that this migration unblocks.

Generated by manage.py makemigrations (effectively); the only schema
change is the choices update on the status CharField.
"""
from __future__ import annotations

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("paper_trail", "0004_papertrailentry_test_case_autoissue_id"),
    ]

    operations = [
        migrations.AlterField(
            model_name="papertrailentry",
            name="status",
            field=models.CharField(
                choices=[
                    ("open", "Open"),
                    ("picked", "Picked"),
                    ("in_progress", "In progress"),
                    ("blocked", "Blocked"),
                    ("deferred", "Deferred"),
                    ("resolved", "Resolved"),
                    ("wontfix", "Wontfix"),
                    ("rejected", "Rejected"),
                    ("duplicate", "Duplicate"),
                    ("stale", "Stale"),
                    ("superseded", "Superseded"),
                    ("sticky", "Sticky"),
                ],
                default="open",
                max_length=16,
            ),
        ),
    ]
