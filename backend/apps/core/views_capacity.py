"""
Historical home of jobs/helpers/auth/analytics views.
Decomposed into views_jobs.py, views_helpers.py, views_ml_settings.py, etc.
on 2026-05-10 to stay under the 1500-line cap.
"""
# Empty — all symbols moved to domain-specific files.
# Importers should use `from apps.core.views import ...` which re-exports
# everything from the new modules.
