"""Slice 1.6 — private sidecars clients for apps.ops_feed.

Hosts the Python client for bullboard (the rolling "what is happening now"
feed + threshold-rule promotion engine). Moves to apps.operations._sidecars
once slice 9 creates the canonical operations module (paper-trail #571).
"""
